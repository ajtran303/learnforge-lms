<div class="container mx-auto">

<div class="course-overview-header d-flex justify-content-around p-4">
  <h1 class="text-center my-auto"><%= @course.title %></h1>
  <%= render("shared/course_progress", course: @course, user: current_user) if current_user.learner? %>
</div>

<p class="text-center"><%= @course.description %></p>

<div id="flash">
  <%= render "shared/flash" %>
</div>

<% if current_user&.instructor? %>
  <div class="d-flex justify-content-center">
    <p class="me-4 mt-3 mb-3">Status: <%= @course.status.capitalize %></p>
    <% if @course.draft? %>
      <%= button_to "Publish", publish_course_path(@course), method: :patch, class: "btn btn-primary h-100 ms-4" %>
    <% else %>
      <%= button_to "Unpublish", unpublish_course_path(@course), method: :patch, class: "btn btn-secondary h-100 ms-4" %>
    <% end %>
  </div>
  <br>

  <turbo-frame id="lesson_form">
    <%= render "lessons/form", lesson: Lesson.new(course: @course) || Lesson.new(course: @course) %>
  </turbo-frame>
<% end %>

<% if current_user.role == "learner" %>
  <p class="text-center"><strong>Instructor:</strong> <%= @course.user.email %></p>

  <turbo-frame id="enrollment_status">
    <%= render "courses/enrollment_status", course: @course %>
  </turbo-frame>
  <br>
<% end %>

<turbo-frame id="course_lessons">
  <% if current_user.instructor? || current_user.enrolled_courses.include?(@course) %>
    <h2 class="text-center">Lessons</h2>

    <turbo-frame id="lessons">
      <%= render "lessons/lessons", lessons: @course.lessons %>
    </turbo-frame>
  <% end %>
</turbo-frame>

</div>
